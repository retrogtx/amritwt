import { BlogPosts } from 'app/components/posts'

export const metadata = {
    title: 'about',
    description: 'who is amrit?',
}

export default function Page() {
    return (
        <section>
            <h1 className="font-semibold text-2xl mb-8 tracking-tighter">damn, who's this?</h1>
        </section>
    )
}
